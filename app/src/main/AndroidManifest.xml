<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.motionlab.qurqah">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LojinActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QurbanActivity" />
        <activity android:name=".SapiActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".SapiBayarActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".TerimaKasihActivity" />
        <activity android:name=".ComingSoonActivity" />
        <activity android:name=".KambingActivity" />
        <activity android:name=".KambingBayarActivity" />
        <activity android:name=".JenisSapiActivity" />
        <activity android:name=".JenisKambingActivity" />
        <activity android:name=".SapiKecilActivity" />
        <activity android:name=".KambingKecilActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".BelumTersediaActivity"></activity>
    </application>

</manifest>